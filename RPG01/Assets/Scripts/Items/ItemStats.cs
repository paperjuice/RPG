public enum ItemStats {
    TYPE,
    MIN_DAMAGE,
    MAX_DAMAGE,
    HEALTH,
    TOTAL
}

public enum ItemType {
     HEAD,
     CHEST,
     BELT,
     PANTS,
     BOOTS,
     HANDS,
     SHOULDERS,
     RING,
     NECKLACE,
     MAIN_HAND,
     OFF_HAND,
     TOTAL
}